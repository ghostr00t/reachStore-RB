<% provide(:page_title, @listing.title) %>
<% provide(:shop_active, 'active') %>

<div class="row">
  <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2 text-xs-center">
    <h1><%= @listing.title %></h1>
    <p>$100</p>
    <%= @listing.description %>
  </div>
</div>
